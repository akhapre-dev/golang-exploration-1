# Template for .env — safe to commit.
# Copy this file to .env and fill in real values.

GOOGLE_API_KEY=your-google-api-key-here

# Optional overrides (see pkg/config/config.go for defaults)
# AGENT_MODEL=gemini-2.5-flash
# AGENT_NAME=exploration_agent
# AGENT_INSTRUCTION=You are a helpful assistant. Answer user questions clearly and concisely.
